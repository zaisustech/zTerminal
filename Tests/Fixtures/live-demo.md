# Live Reload Demo — complete ✅

The file was just **atomically replaced** (write temp + rename), the way
editors and AI agents save. The watcher re-armed on the new inode and the
preview still updates.

> Tip
> This is exactly what happens when Claude edits your specs while you read them.
> dmoe
> Okay
